name: Deploy to server #nombre del workflow

on: push #evento que dispara el workflow

jobs:
  first_job: 
    runs-on: ubuntu-latest #especifica el sistema operativo en el que se ejecutará el job
    
    steps:
      - name: Hola mundo
        run: echo "Hola mundo" #comando que se ejecutará en el job

  # Puedes agregar más jobs aquí
  second_job: 
    needs: first_job # indica que este job depende de first_job, si hay mas jobs, se hace una lista 
    #ejemplo:
    # needs: 
    #   - first_job
    #   - third_job
    runs-on: ubuntu-latest #especifica el sistema operativo en el que se ejecutará el job
    
    steps:
      - name: Hola mundo
        run: echo "Hola mundo 2" #comando que se ejecutará en el job